body {
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background-color: black;
}

.container {
    text-align: center;
    position: relative;
}

.typing-title {
    color: white;
    font-size: 36px;
    font-family: Arial, sans-serif;
    white-space: nowrap;
    overflow: hidden;
    border-right: 3px solid white;
    animation: typing 3s steps(40) infinite, cursor 0.5s infinite;
}

@keyframes typing {
    from, to {
        width: 0;
    }
    50% {
        width: 100%;
    }
}

@keyframes cursor {
    from, to {
        border-color: transparent;
    }
    50% {
        border-color: white;
    }
}

.buy-button {
    margin-top: 20px;
    padding: 10px 20px;
    font-size: 18px;
    background-color: transparent;
    color: white;
    border: 2px solid white;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s, color 0.3s;
}

.buy-button:hover {
    background-color: white;
    color: black;
}

.discord-contact {
    position: absolute;
    top: 10px;
    right: 10px;
    color: white;
    font-size: 12px;
}
